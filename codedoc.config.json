{
  "llm_mode": "api",
  "llm_provider": "auto",
  "model_name": "",
  "api_key": null,
  "analysis_mode": "single",
  "response_correction_enabled": false,
  "output_format": "both",
  "documentation_scope": "all",
  "prompt_profiles": {
    "$comment": [
      "Public end-to-end fixture for testing extension-scoped prompt profiles.",
      "Python files use the complete .py replacements below; other configured extensions fall back to common."
    ],
    "single": {
      "common": {
        "requested_shape": {
          "description": "Explain what this file does and why it exists based only on visible source evidence."
        }
      },
      "per_extension": {
        ".py": {
          "requested_shape": {
            "description": "For this Python file, explain its concrete purpose and explicitly describe any decorators, type annotations, module-level execution behavior, and Python-specific runtime conventions that are visible in the source. Do not infer details that the code does not establish."
          }
        }
      }
    },
    "triple": {
      "common": {
        "structure": {
          "requested_shape": {
            "description": "Describe the file's visible structure and purpose."
          }
        },
        "dependency": {
          "requested_shape": {
            "dependencies_analysis": {}
          }
        },
        "documentation": {
          "requested_shape": {
            "description": "Explain what this file does and why it exists based only on visible source evidence."
          }
        }
      },
      "per_extension": {
        ".py": {
          "structure": {
            "requested_shape": {
              "description": "For this Python file, describe its structural purpose and explicitly identify visible decorators, type annotations, classes, functions, and module-level execution behavior."
            }
          },
          "dependency": {
            "requested_shape": {
              "dependencies_analysis": {
                "warnings": [
                  "For this Python file, report only visible dependency concerns and distinguish standard-library, local-package, and third-party imports when the source supports that distinction."
                ]
              }
            }
          },
          "documentation": {
            "requested_shape": {
              "description": "For this Python file, provide a factual developer-facing explanation that explicitly covers visible decorators, type annotations, module-level behavior, and Python-specific runtime conventions."
            }
          }
        }
      }
    }
  }
}
